// This file is quite useless... make it something admirable?
export default function playerConnected(data) {
    // const { socket } = window.CONQUEST;

    console.log('playerConnected', data);

    console.log(window.CONQUEST.players);
}